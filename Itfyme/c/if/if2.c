#include<stdio.h>
int main()
{
    int score=55;
    
    if(score<=55)
    {
    printf("failing %d\n",score);
    }
    else
{
    if(score<=65)
  {
    printf("unsatisfactory%d",score);
  }else
  printf("satisfactory",score);
}
}
    