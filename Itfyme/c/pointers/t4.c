#include<stdio.h>
#include<stdlib.h>
int main(){
    int a;
    int b;
    printf("enter the 2 num to find maximum\n");
    scanf("%d %d",&a,&b);
    p=&a;
    p=&b;
    if(p=>p){
        printf("max num is %d",p);


    }else{
        printf("max num is %d",p);
    }
}